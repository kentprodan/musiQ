package com.musiq.app.ui.theme

import androidx.compose.material3.Typography

// Left at Material 3's default type ramp (Roboto Flex) — the scaffold's
// job is the Dynamic Color wiring in Theme.kt, not a custom type scale.
val MusiqTypography = Typography()
