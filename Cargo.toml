[workspace]
resolver = "2"
members = ["core/musiq-core", "ffi/musiq-uniffi"]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Kent Prodan"]
license = "MIT"
repository = "https://github.com/kentprodan/musiQ"

[profile.release]
lto = true
codegen-units = 1
strip = true
